"""Service layer: use-cases orchestrating domain logic over core ports.

Services receive their dependencies (ports) via constructor injection — they never
instantiate adapters. Populated feature-by-feature (F3+).
"""
